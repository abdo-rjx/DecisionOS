-- DecisionOS Flyway migrations placeholder (Phase 0 scaffolding).
-- No entities exist yet; the first versioned migration will be added in Phase 1
-- alongside the Organization entity. This file keeps the db/migration location present.
SELECT 1;
